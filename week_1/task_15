#include <iostream>
using namespace std;

int main() {
    char c;
    int res = 0;

    do {
        cin >> c;
        if (c >= '0' and c <= '9')
            res++;
    } while (c != '@');

    cout << res;
    return 0;
}
