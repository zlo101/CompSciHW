#include <iostream>
using namespace std;

int main() {
    int n, m, tmp;

    cin >> n;
    int arr[n];
    for (int i = 0; i < n; i++) {
        cin >> arr[i];
    }
    cin >> m;

    int a[m][2];
    a[0][0] = INT_MIN;
    a[0][1] = -1;

    for (int j = 0; j < m; j++) {
        
    }

    for (int i=1; i<=n; i++) {
        if (tmp > arr[i])
            tmp = arr[i];
    }
    cout << tmp;
}
