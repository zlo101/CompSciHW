#include <iostream>
using namespace std;

unsigned int sum_of_numbers(unsigned int n) {
    int ans = 0;
    while (n) {
        ans += n % 10;
        n /= 10;
    }
    return ans;
}

int main() {
    unsigned int n;
    cin >>n;
    cout <<sum_of_numbers(n) <<endl;
    return 0;
}
